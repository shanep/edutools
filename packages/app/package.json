{
  "name": "@edutools/app",
  "productName": "edutools",
  "version": "0.1.0",
  "description": "Manage Canvas course design from the desktop",
  "author": "Shane Panter <shanepanter@boisestate.edu>",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.web.json --noEmit",
    "build": "electron-vite build",
    "dist": "npm run build && electron-builder --config electron-builder.config.cjs",
    "dist:dir": "npm run build && electron-builder --config electron-builder.config.cjs --dir",
    "dist:mac": "node scripts/native-deps.mjs darwin-arm64 darwin-x64 && npm run build && electron-builder --config electron-builder.config.cjs --mac --arm64 --x64",
    "dist:win": "node scripts/native-deps.mjs win32-x64-msvc && npm run build && electron-builder --config electron-builder.config.cjs --win --x64"
  },
  "dependencies": {
    "@napi-rs/keyring": "^2.1.0"
  },
  "devDependencies": {
    "@edutools/core": "*",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^44.4.5",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "vite": "^7.3.6"
  }
}
